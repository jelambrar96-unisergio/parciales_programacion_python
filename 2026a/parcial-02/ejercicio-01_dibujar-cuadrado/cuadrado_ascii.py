"""
Módulo para dibujar cuadrados usando caracteres ASCII.

Este módulo proporciona funciones para generar representaciones
visuales de cuadrados usando caracteres ASCII personalizables.
"""


def dibujar_cuadrado(lado, symbolo, separador=""):
    """
    Dibuja un cuadrado usando caracteres ASCII.
    
    Args:
        lado (int): El número de caracteres por lado del cuadrado.
        symbolo (str): El carácter a utilizar para dibujar el cuadrado.
        separador (str, optional): Caracteres entre símbolos en cada fila.
                                   Por defecto es vacío ("").
    
    Returns:
        str: Una cadena que representa el cuadrado ASCII.
    
    Ejemplo:
        >>> print(dibujar_cuadrado(3, "*"))
        ***
        ***
        ***
    """
    # TODO: reemplaza la linea pass por tu codigo
    pass
